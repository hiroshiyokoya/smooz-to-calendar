# ローカル専用。このファイルをコピーして .gcp.env を作り、値を記入する。
# .gcp.env は gitignore 済みで、公開リポジトリには含めない。
#
# プロジェクトIDの確認:
#   gcloud projects list
#   gcloud config get-value project
# Cloud Console: https://console.cloud.google.com/cloud-resource-manager

GCP_PROJECT_ID=your-gcp-project-id
